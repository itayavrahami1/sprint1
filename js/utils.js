'use strict';

function _getRandomIntInclusive(min, max) {
    return Math.floor(Math.random() * (max - min + 1)) + min;
}

function _getMouseBtn(elCell) {

    // var btn = elCell.addEventListener('mouseup', logButtons)
    // console.log(btn.data);
    // // console.log(elCell);
    // var btn;
    // var btnType;
    // elCell.addEventListener('mouseup', logButtons);
    // elCell.addEventListener('mousedown', logButtons);
    // function logButtons(e) {
    //     btn = e.buttons;
    //     btnType = e.type;
    // }

    // console.log(elCell);


    // elCell.addEventListener('mouseup', logMouseButton);
    // var mouseBtn;


    // function logMouseButton(e) {
    //     console.log(e);
    //     if (typeof e === 'object') {
    //         switch (e.button) {
    //             case 0:
    //                 mouseBtn = 'Left';
    //                 console.log(mouseBtn);
    //                 break;
    //             case 2:
    //                 mouseBtn = 'Right';
    //                 console.log(mouseBtn);
    //                 break;
    //         }
    //     }
    // }
}